<Page
    x:Class="Assignment_UWP.Pages.ListSongPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Assignment_UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:Assignment_UWP.Entity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <ListView x:ConnectionId='11' x:Name="ListViewSong">
                <ListView.ItemTemplate>
                    <DataTemplate                         >
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Height="60" Width="60">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding thumbnail}" />
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding name}"></TextBlock>
                            <SymbolIcon x:ConnectionId='14' Symbol="Play"                                         Tag="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <CommandBar VerticalAlignment="Bottom">
            <AppBarToggleButton x:ConnectionId='3' Icon="Shuffle" Label="Shuffle"                            />
            <AppBarToggleButton x:ConnectionId='4' Icon="RepeatAll" Label="Repeat"                           />
            <AppBarSeparator/>
            <AppBarButton x:ConnectionId='5' Icon="Previous" Label="Previous"                 />
            <AppBarButton x:ConnectionId='6' Icon="Play" x:Name="PlayAndPauseButton" Label="Play"             />
            <AppBarButton x:ConnectionId='7' Icon="Next" Label="Next"             />

            <CommandBar.SecondaryCommands>
                <AppBarButton x:ConnectionId='8' Label="Like"                           />
                <AppBarButton x:ConnectionId='9' Label="Dislike"                           />
            </CommandBar.SecondaryCommands>

            <CommandBar.Content>
                <TextBlock x:ConnectionId='10' x:Name="NowPlayingText" Text="Now playing..." Margin="12,14"/>
            </CommandBar.Content>
        </CommandBar>
        <MediaElement x:ConnectionId='2' x:Name="MyMediaElement"></MediaElement>
    </Grid>
</Page>

